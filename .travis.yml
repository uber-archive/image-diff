language: node_js

sudo: false

node_js:
  - "0.12"
  - "0.10"
  - "iojs"

matrix:
  fast_finish: true

install:
  # Output imagemagick version
  - convert -version

  # Install dependencies
  - npm install

notifications:
  email: false
